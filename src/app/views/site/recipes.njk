{% extends 'layout.njk' %}

{% block styles %}
    <link rel="stylesheet" href="styles/recipes.css">
{% endblock styles %}

{% block header %}
    <form action="/recipes" method="get" id="search">
        <input type="text" name="filter" value="{{ filter }}" placeholder="Buscar uma receita">
        <button type="submit" class="button">
            <span class="material-icons">
                search
            </span>
        </button>
    </form>
{% endblock header %}

{% block content %}
    {% if filter %}
    <h2>Buscando por "{{ filter }}"</h2>
    {% endif %}
    <div class="cards-container">

        {% for recipe in recipes %}
            <div class="card-admin recipes-page" id="{{ recipe.id}}">
                <img src="{{ recipe.image }}" alt="{{ recipe.title }}">
                <div class="title">
                    <h4>{{ recipe.title }}<h4>                        
                    <p>Por <strong>{{ recipe.chef_name }}</strong></p>
                </div>
                    </div>
                {% endfor %}

            </div>

        {% endblock content %}